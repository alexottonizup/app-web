name: "PR Release → Main"

on:
  push:
    branches:
      - 'release/**'

permissions:
  contents: write
  pull-requests: write

jobs:
  call-template:
    uses: alexottonizup/git-flow-templates/.github/workflows/template-pr-release-main.yml@v1
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}