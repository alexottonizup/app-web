name: "PR Develop → Release"

on:
  push:
    branches:
      - develop

permissions:
  contents: write
  pull-requests: write

jobs:
  call-template:
    uses: alexottonizup/git-flow-templates/.github/workflows/template-pr-develop-release.yml@v1
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}