name: "PR Feature → Develop (via template)"

on:
  push:
    branches:
      - "feature/**"
  workflow_dispatch:

jobs:
  pr-feature-develop:
    uses: org/git-flow-templates/.github/workflows/template-pr-feature-develop.yml@main
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
